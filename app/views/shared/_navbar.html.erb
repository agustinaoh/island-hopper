

<nav class="navbar navbar-expand-lg bg-primary sticky-top">

  <div class="container d-flex justify-content-between">

    <!-- LOGO PART -->
    <div class="logo-box">
      <%= link_to root_path do %>
      <%= image_tag "logo_nav.png", class: "d-inline-block align-top logo-img-nav" %>
      <span class="navbar-brand logo-text-nav">Island Hopper</span>
      <% end %>
    </div>

    <!-- MENU PART -->
    <div>
      <div class="btn-group" id="navbar">
        <button type="button" class="btn dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <% if (user_signed_in?) %>
            <span class="text-light pr-3"><%= current_user.first_name %></span>
            <%= cl_image_tag current_user.avatar.key, class: "nav-avatar" %>
          <% else %>
            <%= image_tag "default_avatar.png", class: "nav-avatar" %>
  <!--    <img src="https://cdn.icon-icons.com/icons2/1378/PNG/512/avatardefault_92824.png" class="nav-avatar" alt=""> -->      <% end  %>
        </button>

        <div class="dropdown-menu p-3">
          <% if (!user_signed_in?) %>
            <a class="dropdown-item" href="#"><%= link_to 'Log In', new_user_session_path %></a>
            <a class="dropdown-item" href="#"> <%= link_to 'Sign Up', new_user_registration_path %></a>
          <% else %>

            <%= link_to 'Dashboard', dashboard_path, class: "dropdown-item" %>

            <%= link_to 'Edit Profile',edit_user_registration_path, class: "dropdown-item"  %>

            <div class="dropdown-divider"></div>

            <div class="d-flex">
<!--          <i class="fas fa-sign-out-alt"></i>  -->
              <%= link_to 'Log Out',destroy_user_session_path, method: :delete, class: "dropdown-item"  %>
            </div>
          <%end %>
        </div>
      </div><!--/.nav-collapse -->
    </div>


  </div>

</nav>


